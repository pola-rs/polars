# Baseline of known-failing sqllogictest records (the ratchet).
#
# of the .slt file relative to the `slt/` directory and <line> is the 1-based
# line number of the `query`/`statement` record within that file.
# Blank lines and lines starting with `#` are ignored.
#
#   - a failing record NOT listed here -> the run fails (add a fix, or add the entry).
#   - an entry listed here whose record now PASSES -> the run fails; remove the entry.
#
# The own-authored `slt/polars/` corpus must stay fully green, so it contributes
# no entries here.
#
# Entries below come from the vendored sqlite/ corpus (see slt/sqlite/UPSTREAM).
# Grouped by file; see the Phase 2 triage in the vendoring commit message for the
# ranked breakdown of failure modes these entries represent.

# sqlite/in1.slt (4 entries)

# sqlite/in2.slt (3 entries)

# sqlite/random/aggregates_slt_good_0.slt (12 entries)

# sqlite/random/aggregates_slt_good_50.slt (9 entries)




# sqlite/slt_lang_aggfunc.slt (0 entries)

# sqlite/slt_lang_createview.slt (0 entries)

# Semantics enforced by the runner:
sqlite/in1.slt:281
sqlite/in1.slt:292
sqlite/in1.slt:315
sqlite/in1.slt:326
sqlite/random/aggregates_slt_good_0.slt:206
sqlite/random/aggregates_slt_good_0.slt:433
sqlite/random/aggregates_slt_good_0.slt:655
sqlite/random/aggregates_slt_good_0.slt:787
sqlite/random/aggregates_slt_good_50.slt:302




